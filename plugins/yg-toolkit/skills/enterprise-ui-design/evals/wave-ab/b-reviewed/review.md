# B 版修订稿定向复核

结论：通过

证据边界：

- 已体验内容：本地浏览器实际执行成功下达、部分失败后调整并重新校验、以及键盘移除。提交刚开始时，来源复选框、存草稿和下达均禁用；校验结束前下达禁用。失败后移除 `ZC-20260923-007`、加入 `CK-20260923-052`，经“重新校验候选”后才能再次下达，最终进入已下达只读状态。
- 静态推断：检查了 `b-reviewed/index.html` 的状态门禁、锁定条件和 `aria-label`。原型仍为本地模拟，不连接服务端。
- 未验证内容：真实接口的请求幂等、服务端候选重算、并发与角色权限；这些不属于本次定向复核范围。

## 复核

### 上轮阻断：成功后可重复下达 — 已关闭

- 复现结果：成功后状态标签为“已下达”，结果明确说明“现为只读”；下达按钮禁用，“新建波次”入口显示。
- 保护结果：对禁用按钮触发点击不会改变结果；静态门禁也在已下达状态直接返回。

### 上轮重要：提交中防重复、失败调整后的候选重新判定 — 已关闭

- 复现结果：提交处理中显示锁定说明，并禁用来源选择、存草稿和下达。
- 恢复结果：部分失败后下达保持禁用，“重新校验候选”可用；键盘移除失败来源并加入替代来源后，只有完成模拟校验才恢复下达。校验反馈明确标注生产环境应由服务端重新查询。

### 上轮优化：移除按钮缺少来源单号的可访问名称 — 已关闭

- 复现结果：实际 DOM 中装车单移除控件名称为“移除 ZC-20260923-007”，直接出库单为“移除 CK-20260923-041”；键盘焦点加 Enter 可移除指定来源。

本次未发现上述四项范围内仍可证实的问题。
