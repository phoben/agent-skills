# 标准 BOM 维护原型说明

- 以左侧成品定位、右侧版本与完整子件清单的方式组织，减少在编辑前后切换视图的次数。
- 将已有 SKU 规格以只读样式呈现；临时物料规格提供行内编辑，并显式显示其虚拟件属性。
- 保存前固定展示全量子件范围与未保存提示；切换成品或版本、关闭页面时会提醒未保存修改。
- 复制操作先展示对象、保留内容和版本后果，确认后生成 V4 草稿；所有交互仅修改浏览器内存中的模拟数据。

未实现真实权限校验、服务端整树保存、SKU 建档、发布、审批与持久化；原型不连接目标 ERP 或任何真实服务端。
