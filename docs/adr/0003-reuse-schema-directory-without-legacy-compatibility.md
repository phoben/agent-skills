# 新 CLI 复用结构文件目录但不承诺旧技能兼容

DataPull CLI 继续将项目结构文件写入项目根目录的 `.database-schema/`，但其配置模型、文件格式和运行行为由新产品需求独立定义，不承担与现有 `database-schema` 技能互相读取、迁移或保持兼容的责任。这样可延续用户对结构文件位置的认知并避免同时存在两个近义目录，同时允许新 CLI 按完整命令行产品的需要演进；现有技能保持不变。
