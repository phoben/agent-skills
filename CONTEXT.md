# YG Toolkit

本仓库维护可复用、与具体项目无关的 Agent 技能。以下术语用于统一数据库结构提取能力的业务语言。

## Language

**DataPull**：
面向人类交互、同时可被 Agent 自动调用的数据库结构文件拉取命令行产品。
_Avoid_: database-schema 技能、导出脚本

**引导模式**：
用户只启动 DataPull、不指定具体操作时进入的中文交互流程，由 CLI 分步完成选择、校验、提取和结果反馈。
_Avoid_: 默认命令、向导脚本

**命令模式**：
用户或 Agent 通过明确子命令和参数直接执行操作的非交互流程；它与引导模式共享相同的业务规则。
_Avoid_: Agent 模式、无人值守脚本

**Skill 安装目标**：
用户从 DataPull 内置支持清单中选择的 Agent 工具与安装范围组合；CLI 不通过本机目录或进程推断 Agent 是否已安装。
_Avoid_: 已检测 Agent、自动发现目标

**DataPull Skill**：
用于检测运行环境、协助用户安装 DataPull，并指导 Agent 安全调用 CLI 的轻量技能；它不实现连接管理或结构文件拉取。
_Avoid_: DataPull CLI、数据库提取脚本

**登记连接**：
已经保存非秘密服务器、认证引用与连接安全策略的连接配置；它可记录最近使用或收藏的数据库，但不绑定单一数据库。
_Avoid_: 登记数据库、数据源、数据库实例

**全局连接登记簿**：
由当前用户维护、可在多个本地项目中复用的登记连接集合；项目只保存使用该连接生成的结构文件。
_Avoid_: 项目连接配置、项目数据库清单

**连接别名**：
用于从多个登记连接中精确选择唯一连接的用户定义名称。
_Avoid_: 连接关键字、标签

**目标数据库**：
一次结构提取明确指定的真实数据库；它可以来自连接可访问列表、最近使用或收藏项，也可以由用户手工输入。
_Avoid_: 默认数据库、连接数据库

**连接变量**：
登记连接引用的命名配置值，其实际值由用户级秘密配置或运行进程提供，而不保存在连接登记信息中。
_Avoid_: 明文密码、固定密钥

**数据库凭证**：
由当前用户维护、供登记连接完成身份认证的秘密值；它独立于连接信息保存，且不得进入结构文件、日志或命令参数。
_Avoid_: 连接信息、数据库配置

**结构文件集**：
DataPull 按数据库对象类型维护的 DDL 文件集合；不同对象类型可以在不同时间更新，不代表同一时点的完整数据库状态。
_Avoid_: 结构快照、数据库备份

**数据库对象**：
构成数据库结构并可用 DDL 表达的命名实体，例如表、视图、函数、过程、触发器和序列。
_Avoid_: 数据记录、表数据

**结构文件拉取**：
只读取数据库元数据并生成结构文件集的操作，不包含业务数据、账户密码或权限凭据。
_Avoid_: 数据库备份、数据库迁移
