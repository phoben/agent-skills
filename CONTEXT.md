# YG Toolkit

本仓库维护可复用、与具体项目无关的 Agent 技能。以下术语用于统一数据库结构提取能力的业务语言。

## Language

**登记连接**：
已经保存非秘密服务器、认证引用与连接安全策略的连接配置；它不绑定具体数据库。
_Avoid_: 登记数据库、数据源、数据库实例

**连接别名**：
用于从多个登记连接中精确选择唯一连接的用户定义名称。
_Avoid_: 连接关键字、标签

**目标数据库**：
一次结构提取明确指定的真实数据库；同一登记连接可用于多个目标数据库。
_Avoid_: 默认数据库、连接数据库

**连接变量**：
登记数据库引用的命名配置值，其实际值由项目环境或运行进程提供，而不保存在连接登记信息中。
_Avoid_: 明文密码、固定密钥

**结构快照**：
某个连接别名与目标数据库组合在一次成功提取时形成的完整 DDL 文件集合；每个组合只保留最近一次完整成功的结果。
_Avoid_: 备份、数据导出

**数据库对象**：
构成数据库结构并可用 DDL 表达的命名实体，例如表、视图、函数、过程、触发器和序列。
_Avoid_: 数据记录、表数据

**结构提取**：
只读取数据库元数据并生成结构快照的操作，不包含业务数据、账户密码或权限凭据。
_Avoid_: 数据库备份、数据库迁移
